export interface ICardImage {
  /**
   * src url of image.
   */
  src: string;
  /**
   * Alternative text for image.
   */
  alt: string;
  /**
   * Additional CSS classes.
   */
  className?: string;
}
