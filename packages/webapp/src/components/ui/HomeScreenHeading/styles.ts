import styled from "styled-components";

export const SCHomeScreenHeading = styled.h1`
  font-size: 2.5rem;
  text-align: center;
  u {
    text-decoration-color: ${({ theme }) => theme.palette.primary.main};
  }
`;
